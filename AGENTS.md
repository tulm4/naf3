<!-- code-review-graph MCP tools -->
<!-- IMPORTANT: MCP tool documentation lives in `.cursor/rules/mcp-master-rules.mdc`.
     That file is the canonical source — always read it for the correct tool names
     (always use `_tool` suffix) and auto-trigger rules. This AGENTS.md is kept
     minimal to avoid version skew. -->
